server.port=8081

spring.application.name=auth-service

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/atm_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Ijse@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# JWT
jwt.secret=ATM-Security-JWT-Secret-Key-256bit-Enterprise-Grade-2024!!
jwt.access-token-expiration-ms=900000
jwt.refresh-token-expiration-ms=604800000
jwt.issuer=atm-security-system

# AES encryption for sensitive fields
encryption.aes-secret=ATM-Security-AES-256-Key-Change-In-Prod!!

# Security lockout
auth.max-failed-attempts=5

management.endpoints.web.exposure.include=health,info
